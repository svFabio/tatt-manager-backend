import { Request, Response, NextFunction } from 'express';export const tenantMiddleware = (req: Request, res: Response, next: NextFunction) => {    const negocioId = req.usuario?.negocioId;    if (!negocioId) {        return res.status(401).json({ error: 'No se pudo identificar el negocio.' });    }    req.negocioId = negocioId;    next();};